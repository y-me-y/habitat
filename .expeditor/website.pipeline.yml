steps:
  - label: "Deploy the website :pen:"
    command:
      - cd terraform/habitat-sh-deploy && make apply
    timeout_in_minutes: 10
    retry:
      automatic:
        limit: 1
    expeditor:
      executor:
        docker: