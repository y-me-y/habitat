steps:
  - label: "Deploy the website :pen:"
    command:
      - .expeditor/scripts/deploy_website.sh
    timeout_in_minutes: 10
    retry:
      automatic:
        limit: 1
    expeditor:
      accounts:
        - aws/chef-cd
        - github
      executor:
        docker:
          environment:
            - CHANNEL="dev"
